Step 1 Install external api's
    pip install pandas
    pip install sqlite3

Step 2 Run Code
    python scripts/main.py

    or 

    python main.py 
    (if inside scripts)

Step 3 Look Inside Excel for Other Files
